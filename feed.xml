<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://yamajiang.github.io/feed.xml" rel="self" type="application/atom+xml"/><link href="https://yamajiang.github.io/" rel="alternate" type="text/html" hreflang="en"/><updated>2026-08-03T23:40:20+00:00</updated><id>https://yamajiang.github.io/feed.xml</id><title type="html">blank</title><subtitle>wip. </subtitle><entry><title type="html">Data Structures &amp;amp; Algorithms</title><link href="https://yamajiang.github.io/blog/2026/datastructuresalgo/" rel="alternate" type="text/html" title="Data Structures &amp;amp; Algorithms"/><published>2026-08-03T00:00:00+00:00</published><updated>2026-08-03T00:00:00+00:00</updated><id>https://yamajiang.github.io/blog/2026/datastructuresalgo</id><content type="html" xml:base="https://yamajiang.github.io/blog/2026/datastructuresalgo/"><![CDATA[<h2 id="introduction">Introduction</h2> <h3 id="big-o-notation">Big O Notation</h3> <p>Big O notation is an important concept in computer science. It is used to help us describe the time complexity and space complexity for a particular algorithm.</p> <ul> <li><strong>Time Complexity</strong>: how the run time changes as the input size (<code class="language-plaintext highlighter-rouge">n</code>) changes</li> <li><strong>Space Complexity</strong>: how much memory the algorithm requires when input size (<code class="language-plaintext highlighter-rouge">n</code>) changes</li> <li>Big O notation describes the asymptotic upper bound on an algorithm’s growth rate as the input size (n) becomes very large. It is commonly used to describe worst case time or space complexity, although it can also describe best- ase or average case complexity when specified</li> <li>We ignore constants and lower order terms because they become insignificant as n grows larger</li> <li>Algorithms can be analyzed using three cases: <strong>Best Case</strong>, <strong>Average Case</strong>, and <strong>Worst Case</strong>. These cases may differ depending on the algorithm and input conditions</li> <li><strong>Worst-case scenario</strong> is typically chosen to represent an algorithm because it provides a reliable upper bound on performance.</li> </ul> <h4 id="growth-rates">Growth Rates</h4> <p>Fastest → Slowest:</p> <p><code class="language-plaintext highlighter-rouge">O(1)</code> → <code class="language-plaintext highlighter-rouge">O(log n)</code> → <code class="language-plaintext highlighter-rouge">O(n)</code> → <code class="language-plaintext highlighter-rouge">O(n log n)</code> → <code class="language-plaintext highlighter-rouge">O(n²)</code> → <code class="language-plaintext highlighter-rouge">O(2ⁿ)</code> → <code class="language-plaintext highlighter-rouge">O(n!)</code></p> <p>As n becomes larger, algorithms with slower growth rates generally scale better</p> <h4 id="properties-of-big-o-notation">Properties of Big O Notation</h4> <ul> <li><strong>Reflexivity:</strong> A function is always Big O of itself (For function <code class="language-plaintext highlighter-rouge">f(n)</code>, <code class="language-plaintext highlighter-rouge">f(n) = O(f(n))</code>)</li> <li><strong>Transitivity:</strong> If <code class="language-plaintext highlighter-rouge">f(n) = O(g(n))</code> and <code class="language-plaintext highlighter-rouge">g(n) = O(h(n))</code>, then <code class="language-plaintext highlighter-rouge">f(n) = O(h(n))</code></li> <li><strong>Constant Factor Rule:</strong> Constants can be ignored because they do not affect growth rate (<code class="language-plaintext highlighter-rouge">O(2n)</code> → <code class="language-plaintext highlighter-rouge">O(n)</code>)</li> <li><strong>Sum Rule:</strong> When adding complexities, keep only the dominant term (<code class="language-plaintext highlighter-rouge">O(n² + n)</code> → <code class="language-plaintext highlighter-rouge">O(n²)</code>)</li> <li><strong>Product Rule:</strong> When multiplying operations, multiply their complexities (<code class="language-plaintext highlighter-rouge">O(n) * O(n)</code> → <code class="language-plaintext highlighter-rouge">O(n²)</code>)</li> <li><strong>Composition Rule:</strong> When one operation is performed inside another (such as nested loops or a function called repeatedly inside a loop), multiply their complexities (If <code class="language-plaintext highlighter-rouge">f(n) = O(g(n))</code>, then <code class="language-plaintext highlighter-rouge">f(h(n)) = O(g(h(n)))</code>)</li> </ul> <h4 id="analyzing-time-complexity">Analyzing Time Complexity</h4> <p>Time complexity is calculated by counting how the number of operations executed by the algorithm changes as input size (<code class="language-plaintext highlighter-rouge">n</code>) changes</p> <p><strong>General Steps</strong>:</p> <ol> <li>Identify the basic operations performed by the algorithm</li> <li>Determine how many times each operation executes relative to n</li> <li>Analyze loops, nested loops, conditionals, and recursive calls</li> <li>Combine the resulting complexities</li> <li>Remove constant factors and lower order terms</li> <li>Express the result using Big O notation</li> </ol> <p><strong>Common Patterns</strong>:</p> <ul> <li>Single operation → <code class="language-plaintext highlighter-rouge">O(1)</code></li> <li>Single loop → <code class="language-plaintext highlighter-rouge">O(n)</code></li> <li>Two consecutive loops → <code class="language-plaintext highlighter-rouge">O(n)</code></li> <li>Nested loops → <code class="language-plaintext highlighter-rouge">O(n²)</code></li> <li>Three nested loops → <code class="language-plaintext highlighter-rouge">O(n³)</code></li> <li>Divide the input in half each iteration → <code class="language-plaintext highlighter-rouge">O(log n)</code></li> <li>Loop with a logarithmic operation → <code class="language-plaintext highlighter-rouge">O(n log n)</code></li> <li>Recursive branching into two calls → Often <code class="language-plaintext highlighter-rouge">O(2ⁿ)</code></li> <li>Generate every permutation → <code class="language-plaintext highlighter-rouge">O(n!)</code></li> </ul> <h4 id="analyzing-space-complexity">Analyzing Space Complexity</h4> <p>Space complexity measures how the amount of memory an algorithm requires changes as the input size (<code class="language-plaintext highlighter-rouge">n</code>) changes</p> <p>When analyzing space complexity, we focus on the additional memory required by the algorithm like variables, arrays, lists, hash tables, and recursive call stacks.</p> <p><strong>General Steps</strong>:</p> <ol> <li>Identify the additional memory allocated by the algorithm</li> <li>Determine whether that memory grows with the input size (n)</li> <li>Analyze data structures such as arrays, lists, and hash tables</li> <li>Analyze recursive algorithms because each recursive call consumes stack space</li> <li>Ignore constant factors and lower order terms</li> <li>Express the result using Big O notation</li> </ol> <h4 id="common-big-o-complexities">Common Big O Complexities</h4> <ul> <li>Constant Time (<code class="language-plaintext highlighter-rouge">O(1)</code>)</li> <li>Linear Time (<code class="language-plaintext highlighter-rouge">O(n)</code>)</li> <li>Logarithmic Time (<code class="language-plaintext highlighter-rouge">O(log n)</code>)</li> <li>Linearithmic Time (<code class="language-plaintext highlighter-rouge">O(n log n)</code>)</li> <li>Quadratic Time (<code class="language-plaintext highlighter-rouge">O(n²)</code>)</li> <li>Cubic Time (<code class="language-plaintext highlighter-rouge">O(n³)</code>)</li> <li>Polynomial Time (<code class="language-plaintext highlighter-rouge">O(nᵏ)</code>)</li> <li>Exponential Time (<code class="language-plaintext highlighter-rouge">O(2ⁿ)</code>)</li> <li>Factorial Time (<code class="language-plaintext highlighter-rouge">O(n!)</code>)</li> </ul>]]></content><author><name></name></author><category term="notes"/><summary type="html"><![CDATA[DSA Topics and Notes]]></summary></entry><entry><title type="html">Data Engineering</title><link href="https://yamajiang.github.io/blog/2026/dataengineering/" rel="alternate" type="text/html" title="Data Engineering"/><published>2026-07-14T00:00:00+00:00</published><updated>2026-07-14T00:00:00+00:00</updated><id>https://yamajiang.github.io/blog/2026/dataengineering</id><content type="html" xml:base="https://yamajiang.github.io/blog/2026/dataengineering/"><![CDATA[<p>My goal is to build a practical foundation in:</p> <ul> <li>data modeling</li> <li>data pipelines</li> <li>storage systems</li> <li>orchestration</li> <li>data quality and observability</li> </ul> <h3 id="general-data-workflow">General Data Workflow</h3> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>                               Raw Data
                                   │
                                   ▼
                            Data Engineer
                  • collects, cleans, transforms
                and stores data by building data pipelines
                                   │
                    ┌──────────────┴───────────────┐
                    ▼                              ▼
              Data Analyst                   Data Scientist
          • analyzes and queries       • uses statistic and ml to
        data to produce reports,     build predictive models test
      metrics, and visualizations    hypothesis, and uncover patterns
                    │                               │
                    └──────────────┬────────────────┘
                                   ▼
                              ML Engineer
                   • takes validated models &amp; data to 
                  build scalable machine learning models 
                     and maintains ml infrastructure
                                   │
                  ┌────────────────┴──────────────┐
                  ▼                               ▼
        MLOps Platform Engineer             AI / Product Engineer
   • automates deployment, scaling,        • integrates ML/AI into apps using
     monitoring, and CI/CD                  LLMs, APIs, and prompt engineering
                                            to build AI-powered user features
</code></pre></div></div> <h2 id="foundation">Foundation</h2> <p>This section includes notes on foundations and specific tools for data engineering. This is to help build a strong understanding of the fundamental concepts, technologies, and best practices required to design, build, and maintain reliable data pipelines.</p> <p>I used Luke Barousse’s bootcamp to help me with understanding some of the fundamentals.</p> <h3 id="fundamentals">Fundamentals</h3> <h4 id="what-is-data-engineering">What is Data Engineering?</h4> <p>Data engineering is designing, building pipelines, and maintaining systems that move and transform data that can be from multiple different sources so that it is reliable and usable for analytics, machine learning, and products.</p> <p>Most data engineering pipelines follow four stages:</p> <p>Ingestion → Storage → Transformation → Serving</p> <ul> <li><strong>Ingestion</strong>: Data ingestion is the process of collecting data from multiple sources such as databases, APIs, applications, and streaming platforms (in batches or real time). The goal is to bring raw data into a centralized system for processing.</li> <li><strong>Transformation</strong>: Data transformation involves cleaning, validating, and converting raw data into a structured and usable format. This includes removing duplicates, handling missing values, and applying business rules. The transformed data becomes consistent and ready for analysis.</li> <li><strong>Serving</strong>: Data serving is the process of making processed data available to users, dashboards, applications, or machine learning models. It ensures data can be accessed quickly and efficiently. Common serving methods include data warehouses, APIs, and analytics platforms.</li> <li><strong>Storage</strong>: Data storage is where raw and processed data is securely kept for future use. It can include data lakes, data warehouses, or cloud storage systems. Good storage solutions provide scalability, reliability, and fast data retrieval.</li> </ul> <p>In data engineering, ACID transactions (Atomicity, Consistency, Isolation, Durability) help ensure data integrity across large scale pipelines. They guarantee that complex ETL (Extract, Transform, Load) operations are executed reliably, even in the presence of system failures or concurrent access.</p> <ul> <li>Atomicity ensures that all steps in a transaction either complete successfully or are rolled back entirely.</li> <li>Consistency guarantees that every transaction moves the database from one valid state to another while preserving data integrity rules.</li> <li>Isolation prevents concurrent transactions from interfering with each other, ensuring predictable results.</li> <li>Durability ensures that once a transaction is committed, the changes are permanently stored and can be recovered even after a system crash.</li> </ul> <p>Together, these properties enable robust, reliable, and fault-tolerant data processing in modern data engineering systems.</p> <h4 id="medallion-architecture">Medallion Architecture</h4> <p>Medallion data architecture is a design pattern used to logically organize date to help improve structure and quality. It consists of 3 progressive layers:</p> <ol> <li>Bronze Layer (Raw Data) <ul> <li>Stores data exactly how it arrives from sources</li> <li>Raw and unprocessed</li> <li>Original state helps preserve ground truth</li> <li>used for auditing and reprocessing</li> </ul> </li> <li>Silver Layer( Cleaned &amp; Structured Data) <ul> <li>Cleans, filters, validates, and standardizes raw data</li> <li>Removes duplicates</li> <li>Handles any missing values</li> <li>Fix data types and standardize</li> </ul> </li> <li>Gold Layer (Aggregated Data) <ul> <li>Create data that is ready for use for different purposes</li> <li>Feature engineering</li> <li>Aggregations and calculations</li> <li>Metrics and tables</li> <li>KPI (Key performance indicator) - serves as the standardized, analytical calculations that track health and performance.</li> </ul> </li> </ol> <h4 id="normalized-vs-denormalized-tables">Normalized vs Denormalized Tables</h4> <ul> <li>Normalized tables are multiple tables that are linked by keys or id values. Data redundancy is minimized or eliminated, but it is slower because it requires multiple joins.</li> <li>Denormalized tables have fewer and “wider” tables which can happen by merging information that are stored in separate tables together. This intentionally introduces data redundancy for faster queries. The trade off is increased storage usage that is use to keep data synchronized.</li> </ul> <h4 id="oltp-vs-olap">OLTP vs OLAP</h4> <p>OLTP and OLAP are two types of database systems designed for different purposes.</p> <ul> <li>Online Transaction Processing (OLTP) handles real time day to day transactions. <ul> <li>Fast read, write, update, delete operations</li> <li>Slow at scanning and aggregating data</li> <li>Database designs are typically normalized tables</li> <li>Examples: e-commerce orders, ATM transactions, banking,</li> </ul> </li> <li>Online Analytical Processing (OLAP) supports business analysis and decision making by analyzing historical data. <ul> <li>Fast at scanning and aggregating data</li> <li>Slow at handling many updates</li> <li>Database designs are often denormalized</li> <li>Examples: sales reporting, forecasting, business intelligence</li> </ul> </li> </ul> <h4 id="etl-extract-transform-load-vs-elt-extract-load-transform">ETL (Extract Transform Load) vs ELT (Extract Load Transform)</h4> <p><u>ETL (Extract, Transform, Load)</u></p> <ul> <li>Extract data from source systems</li> <li>Transform and clean the data before loading it into the destination</li> <li>Load only processed data into the data warehouse</li> <li>Processing is performed by an external ETL tool or server</li> <li>Common in traditional, on-premises data warehouses</li> <li>Best when data quality, compliance, or storage is a priority</li> </ul> <p>Flow: Source Systems → Extract → Transform → Load → Data Warehouse</p> <p><u>ELT (Extract, Load, Transform)</u></p> <ul> <li>Extract data from source systems</li> <li>Load raw data directly into the data warehouse or lakehouse</li> <li>Transform the data after loading using the warehouse’s compute power</li> <li>Preserves raw data for auditing and reprocessing</li> <li>Common in modern cloud data platforms</li> <li>Better suited for large-scale analytics and machine learning workloads</li> </ul> <p>Flow: Source Systems → Extract → Load → Data Warehouse/Lakehouse → Transform → BI / Analytics / ML</p> <h4 id="orchestration">Orchestration</h4> <p>In charge of when to start jobs, what order to run jobs, what to do if a job fails, and whats the status of the jobs.</p> <ul> <li>Manages and automates data pipeline workflows</li> <li>Determines when jobs should start (schedules or event triggers)</li> <li>Controls the order in which jobs and tasks are executed</li> <li>Manages dependencies between tasks (ensures prerequisite jobs finish first)</li> <li>Handles failures by retrying jobs, sending alerts, or triggering recovery actions</li> <li>Monitors the status and health of pipelines (running, completed, failed)</li> <li>Provides logging, monitoring, and notifications for pipeline execution</li> <li>Ensures data pipelines run reliably and efficiently</li> </ul> <p>This is commonly done by managing every stage of ELT with DAG (Directed Acyclic Graph) where there is a directed flow or connections with no loops back.</p> <p>Common Orchestration Tools:</p> <ul> <li>Apache Airflow</li> <li>Prefect</li> <li>Dagster</li> <li>Azure Data Factory</li> </ul> <h4 id="serving">Serving</h4> <ul> <li>BI/Analytics <ul> <li>Users: Data Analysts, Business Analysts, Data Engineers</li> <li>Provides reports to explain “what happened” and “what is going on.”</li> <li>Interactive dashboards and visualizations for business users to explore data</li> <li>Technology: Power BI, Tableau, Excel, Looker</li> </ul> </li> <li>ML/AI <ul> <li>Users: Data Scientists, AI/ML Engineers</li> <li>Uses curated datasets to build predictive and machine learning models.</li> <li>Develops AI applications and model-driven insights using Python.</li> <li>Technology: Python, Jupyter Notebooks, TensorFlow, PyTorch, Scikit-learn</li> </ul> </li> <li>Reverse ETL <ul> <li>Sends cleaned, transformed, and enriched data from the warehouse back into operational systems.</li> <li>Makes analytics available in day-to-day business tools for sales, marketing, customer support, and operations.</li> <li>Examples: Salesforce, HubSpot, Marketo, Zendesk, Slack</li> </ul> </li> </ul> <h4 id="cicd">CI/CD</h4> <ul> <li>Automates the process of building, testing, and deploying code changes</li> <li>Enables teams to release updates quickly, consistently, and with fewer errors</li> <li>Ensures code is tested before being deployed to production</li> <li>Reduces manual deployment effort and improves software reliability</li> <li>Supports version control integration (e.g., Git) for collaborative development</li> </ul> <p><u>Continuous Integration (CI)</u></p> <ul> <li>Automatically builds and tests code whenever changes are committed</li> <li>Detects bugs and integration issues early</li> <li>Runs unit tests, code quality checks, and validation</li> </ul> <p><u>Continuous Deployment/Delivery (CD)</u></p> <ul> <li>Automatically deploys validated code to development, staging, or production environments</li> <li>Ensures deployments are repeatable and consistent</li> <li>Can include approval gates before production deployment (Continuous Delivery) or fully automated production releases (Continuous Deployment)</li> </ul> <h4 id="data-architecture">Data Architecture</h4> <p>Data architecture is the overall blueprint for how data flows through an organization, from collection to analysis. Data is collected through ingestion using batch or streaming methods, stored in systems such as data lakes, data warehouses, or lakehouses, and processed using ETL or ELT pipelines. Cloud platforms provide the scalability needed to manage these systems. OLTP systems create and manage operational data, while OLAP systems analyze data for insights and reporting. The medallion architecture organizes data into Bronze, Silver, and Gold layers to improve data quality and usability. Together, these components create a reliable system for storing, transforming, and serving data for analytics, applications, and machine learning.</p> <h3 id="data-engineering-workflow">Data Engineering Workflow</h3> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>
                             Data Sources
                   • this is where the data originates  
              ex: apps, robots, equipment, sensors, apis, logs 
                                   │
                                   │
                                   ▼
                               Ingestion
              • where data is collected &amp; moved into storage
        1. batch ingestion: runs on a scheduled interval (hrs, daily) 
          ex: every night, sales are copied to the cloud storage 
        2. streaming ingestion: data is transferred continuously 
          ex: car moves -&gt; GPS updates -&gt; app location and 
              navigation directions update in real time
                                   │
                                   │
                                   ▼
                          Storage (Data Lake)
              • raw data is stored in it original format 
             • scalable storage that holds unstructured, 
               semi structured, and structured data
        ex: JSON, CSV, Parquet, log files, images, videos, XML
                                   │
                                   │
                                   ▼
                     Processing &amp; Transformations
       • raw data is cleaned, validated, transformed for analysis
    ex: remove duplicates, handle missing values, standardize, conversions 
                                   │
                                   │
                                   ▼
                           Data Warehouse
       • processed data stored in structured &amp; optimized database
    • data from multiple sources, cleaned, put into tables for queries 
                                   │
                                   │
                                   ▼
                         Dashboard Analytics
        • different teams can use data for different purposes
    ex: visuals &amp; reports - daily sales, growth trends, performance               

</code></pre></div></div> <h3 id="data-lake">Data Lake</h3> <p>A Data Lake is a large, low cost storage system that stores raw data in its original format, regardless of its structure</p> <ul> <li>Stores structured, semi-structured, and unstructured data</li> <li>Supports many different types of files such as: CSV, JSON, Parquet, images, videos, audio, sensor data, logs</li> <li>Data is stored before being cleaned or transformed</li> <li>Uses Schema-on-Read, meaning the schema is applied only when the data is queried.</li> <li><strong>Best Use Cases</strong>: <ul> <li>Machine Learning</li> <li>Data Science</li> <li>Large scale archival storage</li> <li>Sensor data</li> <li>Log analytics</li> </ul> </li> <li><strong>Advantages</strong>: <ul> <li>Inexpensive storage</li> <li>Highly scalable</li> <li>Supports many data types</li> <li>Retains raw data</li> </ul> </li> <li><strong>Disadvantages</strong>: <ul> <li>Query performance is usually slower than warehouses</li> <li>Data quality is not enforced</li> </ul> </li> </ul> <h3 id="data-warehouse">Data Warehouse</h3> <p>A Data Warehouse is a centralized repository that stores structured, cleaned, and transformed data optimized for analytics</p> <ul> <li>Stores only structured data</li> <li>Data is typically cleaned and transformed before loading (ETL or ELT)</li> <li>Optimized for SQL queries and analytical workloads</li> <li><strong>Best Use Cases</strong>: <ul> <li>Business Intelligence</li> <li>Financial analysis</li> <li>Reporting and dashboards</li> <li>KPI tracking</li> </ul> </li> <li><strong>Advantages</strong>: <ul> <li>Fast SQL query performance</li> <li>High data quality and consistency</li> <li>Easy for analysts and business user</li> </ul> </li> <li><strong>Disadvantages</strong>: <ul> <li>Limited support for unstructured data</li> <li>High implementation costs</li> <li>Less flexible when data formats frequently change</li> <li>Traditional data warehouses rely on batch processing so they are often unable to deliver real time data</li> </ul> </li> </ul> <h3 id="data-lakehouse">Data Lakehouse</h3> <p>A Data Lakehouse combines the flexibility and low-cost storage of a Data Lake with the reliability, governance, and SQL performance of a Data Warehouse.</p> <p>Instead of copying data from a lake into a warehouse, a lakehouse allows analytics and machine learning to operate directly on files stored in the data lake.</p> <ul> <li>Stores raw and processed data in open file formats (typically Parquet)</li> <li>Supports SQL querying directly on data lake files</li> <li>Eliminates the need to maintain separate lake and warehouse systems</li> <li>Provides ACID transactions for reliable updates</li> <li><strong>Best Use Cases</strong>: <ul> <li>Large scale analytics</li> <li>Modern data engineering pipelines</li> <li>Streaming and batch processing</li> <li>Unified data platforms</li> <li>Machine learning workflows</li> <li>AI applications</li> </ul> </li> <li><strong>Advantages</strong>:</li> <li>Combines low-cost storage with high-performance analytics</li> <li>Single platform for analytics, AI, and ML</li> <li>Supports open data formats</li> <li><strong>Disadvantages</strong>: <ul> <li>More complex architecture than a warehouse</li> <li>Managing security, metadata, data quality, and compliance across structured and unstructured data can be challenging</li> <li>Query performance may not always match that of a highly optimized data warehouse, especially for complex analytical workloads (Performance tuning is often required)</li> </ul> </li> </ul> <h2 id="sql">SQL</h2> <p>Structured Query Language (SQL) is used to manage, manipulate, and retrieve data stored in relational databases.</p> <ul> <li>Where does SQL run? <ul> <li>SQL runs on a database management system (DBMS), which can be installed on your local computer or hosted on a remote server. The database engine executes your SQL queries and returns the results.</li> </ul> </li> <li>With SQL, you typically don’t need to worry about data size because modern warehouses automatically scale compute across multiple machines</li> <li>SQL is great working withe structured data like joining tables and aggregating grouping</li> <li>SQL doesn’t do great when classifying messy text, feature engineering, or nested JSON data, which is where Python comes in for transformation (especially for AI and machine learning)</li> </ul> <h3 id="database-hierarchy">Database Hierarchy</h3> <ul> <li>A database can contain multiple schemas</li> <li>Each schema is a logical container within the database that holds its own tables, views, functions, procedures, and other database objects</li> </ul> <h3 id="dbt-databuild-tools">dbt (databuild tools)</h3> <p><strong>What is dbt?</strong></p> <ul> <li>dbt (Data Build Tool) is used to manage and organize SQL based data transformations in a data warehouse.</li> <li>It helps transform raw data into clean, analytics ready datasets using SQL. When to use dbt</li> <li>Use dbt when working with data warehouses that require structured, maintainable SQL transformations.</li> </ul> <p><strong>It is especially useful for projects with:</strong></p> <ul> <li>Many tables</li> <li>Complex transformation logic</li> <li>Multiple developers collaborating</li> <li>Version control and testing requirements</li> </ul> <p><strong>When dbt may not be necessary:</strong></p> <ul> <li>For simple transformations involving only a few tables, writing plain SQL scripts may be sufficient.</li> <li>In small projects, introducing dbt can add unnecessary complexity.</li> </ul> <h3 id="select-from">SELECT FROM</h3> <ul> <li>In order to retrieve or query data from a database, we need to use a <u>SELECT</u> statement</li> <li>A query is a statement used to retrieve, manipulate, or manage data stored within a relational database</li> <li>You can select particular columns from a table: <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>SELECT column, another_column, …
FROM mytable;
</code></pre></div> </div> </li> <li>Or if we want to retrieve all columns of data from a table, we use the asterisk (*): <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>SELECT *
FROM my table;
</code></pre></div> </div> </li> </ul> <h3 id="basic-queries">Basic Queries</h3> <ul> <li><u>WHERE</u> is used to to filter out certain results based on certain conditions <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>SELECT column, another_column, …
FROM mytable
WHERE condition
  AND/OR another_condition
  AND/OR …;
</code></pre></div> </div> </li> <li><u>GROUP BY</u> discards duplicates based on specific columns and used to group rows that have the same values into summary rows, like “Find the number of customers in each country” <ul> <li>Almost always used in conjunction with aggregation functions, like COUNT(), MAX(), MIN(), SUM(), AVG(), to perform calculations on each group</li> <li> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>SELECT column1, aggregate_function(column2), column3, ...
FROM table_name
WHERE condition
GROUP BY column1, column3
ORDER BY column_name;
</code></pre></div> </div> </li> </ul> </li> <li> <p><u>HAVING</u> is used to filter groups after they have been created with GROUP BY. Unlike WHERE, which filters individual rows before grouping, HAVING filters the aggregated results (often using aggregate functions like COUNT(), SUM(), AVG(), etc.).</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>SELECT column1, aggregate_function(column2)
FROM table_name
WHERE condition
GROUP BY column1
HAVING aggregate_function(column2) condition
ORDER BY column1;
</code></pre></div> </div> <p>Example: Find countries that have more than 5 customers.</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>SELECT country, COUNT(*) AS num_customers
FROM customers
GROUP BY country
HAVING COUNT(*) &gt; 5;
</code></pre></div> </div> <ul> <li><u>WHERE</u> → Filters individual rows before grouping</li> <li><u>HAVING</u>→ Filters grouped results after grouping Example using both: <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>SELECT department, AVG(salary) AS avg_salary
FROM employees
WHERE salary &gt; 30000
GROUP BY department
HAVING AVG(salary) &gt; 50000;
</code></pre></div> </div> </li> <li>WHERE salary &gt; 30000 removes employees earning $30,000 or less before grouping</li> <li>HAVING AVG(salary) &gt; 50000 only returns departments whose average salary is greater than $50,000</li> </ul> </li> <li><u>LIMIT</u> limits the particular amount of rows you are querying <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>SELECT column, another_column, …
FROM mytable
LIMIT 4;
</code></pre></div> </div> </li> <li><u>OFFSET</u> is used to specify what row to begin from <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>SELECT column, another_column, …
FROM mytable
WHERE condition(s)
LIMIT num_limit OFFSET num_offset;
</code></pre></div> </div> </li> <li><u> DISTINCT </u> can be used to discard rows that have a duplicate column value <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>SELECT DISTINCT column, another_column, … 
</code></pre></div> </div> </li> <li><u>ORDER BY </u> is used to sort columns by ascending or descending order <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>SELECT column, another_column, …
FROM mytable
WHERE condition(s)
ORDER BY column ASC/DESC;
</code></pre></div> </div> </li> </ul> <h3 id="constraints-and-filtering">Constraints and Filtering</h3> <ul> <li>In order to filter out certain results, we can use the <u>WHERE</u> clause along with certain condition statements <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>SELECT column, another_column, …
FROM mytable
WHERE condition
  AND/OR another_condition
  AND/OR …;
</code></pre></div> </div> </li> <li>Complex clauses can be constructed by joining numerous AND or OR logical keywords</li> </ul> <table> <thead> <tr> <th>Operator</th> <th>Condition</th> <th>SQL Example</th> </tr> </thead> <tbody> <tr> <td><code class="language-plaintext highlighter-rouge">=</code>, <code class="language-plaintext highlighter-rouge">!=</code>, <code class="language-plaintext highlighter-rouge">&lt;</code>, <code class="language-plaintext highlighter-rouge">&lt;=</code>, <code class="language-plaintext highlighter-rouge">&gt;</code>, <code class="language-plaintext highlighter-rouge">&gt;=</code></td> <td>Standard numerical comparison operators</td> <td><code class="language-plaintext highlighter-rouge">col_name != 4</code></td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">BETWEEN ... AND ...</code></td> <td>Number is within a range of two values (inclusive)</td> <td><code class="language-plaintext highlighter-rouge">col_name BETWEEN 1.5 AND 10.5</code></td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">NOT BETWEEN ... AND ...</code></td> <td>Number is not within a range of two values (inclusive)</td> <td><code class="language-plaintext highlighter-rouge">col_name NOT BETWEEN 1 AND 10</code></td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">IN (...)</code></td> <td>Number exists in a list</td> <td><code class="language-plaintext highlighter-rouge">col_name IN (2, 4, 6)</code></td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">NOT IN (...)</code></td> <td>Number does not exist in a list</td> <td><code class="language-plaintext highlighter-rouge">col_name NOT IN (1, 3, 5)</code></td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">=</code></td> <td>Exact string comparison.<br/>Case sensitivity depends on the database/collation.</td> <td><code class="language-plaintext highlighter-rouge">col_name = "abc"</code></td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">!=</code> or <code class="language-plaintext highlighter-rouge">&lt;&gt;</code></td> <td>Exact string inequality comparison.<br/>Case sensitivity depends on the database/collation.</td> <td><code class="language-plaintext highlighter-rouge">col_name != "abcd"</code></td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">LIKE</code></td> <td>Pattern matching.<br/>Without wildcards (<code class="language-plaintext highlighter-rouge">%</code> or <code class="language-plaintext highlighter-rouge">_</code>), it matches the entire string.<br/>Case sensitivity depends on the database/collation.</td> <td><code class="language-plaintext highlighter-rouge">col_name LIKE "ABC"</code></td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">NOT LIKE</code></td> <td>Negates <code class="language-plaintext highlighter-rouge">LIKE</code> pattern matching.<br/>Case sensitivity depends on the database/collation.</td> <td><code class="language-plaintext highlighter-rouge">col_name NOT LIKE "ABCD"</code></td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">%</code></td> <td>Matches zero or more characters.<br/>Used only with <code class="language-plaintext highlighter-rouge">LIKE</code> or <code class="language-plaintext highlighter-rouge">NOT LIKE</code>.</td> <td><code class="language-plaintext highlighter-rouge">col_name LIKE "%AT%"</code><br/>Matches <code class="language-plaintext highlighter-rouge">"AT"</code>, <code class="language-plaintext highlighter-rouge">"ATTIC"</code>, <code class="language-plaintext highlighter-rouge">"CAT"</code>, or <code class="language-plaintext highlighter-rouge">"BATS"</code></td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">_</code></td> <td>Matches exactly one character.<br/>Used only with <code class="language-plaintext highlighter-rouge">LIKE</code> or <code class="language-plaintext highlighter-rouge">NOT LIKE</code>.</td> <td><code class="language-plaintext highlighter-rouge">col_name LIKE "AN_"</code><br/>Matches <code class="language-plaintext highlighter-rouge">"AND"</code>, but not <code class="language-plaintext highlighter-rouge">"AN"</code></td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">IN (...)</code></td> <td>String exists in a list</td> <td><code class="language-plaintext highlighter-rouge">col_name IN ("A", "B", "C")</code></td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">NOT IN (...)</code></td> <td>String does not exist in a list</td> <td><code class="language-plaintext highlighter-rouge">col_name NOT IN ("D", "E", "F")</code></td> </tr> </tbody> </table> <h3 id="joins">Joins</h3> <ul> <li><u>JOIN</u> is used to combine row data across two separate tables based on a related column, or unique key, between them</li> <li><u>INNER JOIN</u> combines rows from two or more tables by looking for matching values in a shared column. It returns only the rows that find a match in both tables, leaving out any data that does not match <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>SELECT column, another_table_column, …
FROM mytable
INNER JOIN another_table 
    ON mytable.id = another_table.id
WHERE condition(s)
ORDER BY column, … ASC/DESC
LIMIT num_limit OFFSET num_offset;
</code></pre></div> </div> <p>Example:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>SELECT employees.name, departments.department_name
FROM employees
INNER JOIN departments
    ON employees.department_id = departments.id;
</code></pre></div> </div> </li> <li><u>LEFT JOIN</u> (or LEFT OUTER JOIN) returns all rows from the left table and the matching rows from the right table. If there is no match, the columns from the right table contain NULL <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>SELECT column, another_table_column, …
FROM mytable
LEFT JOIN another_table
    ON mytable.id = another_table.id
WHERE condition(s)
ORDER BY column ASC/DESC
LIMIT num_limit OFFSET num_offset;
</code></pre></div> </div> <p>Example:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>SELECT employees.name, departments.department_name
FROM employees
LEFT JOIN departments
    ON employees.department_id = departments.id;
</code></pre></div> </div> <p>This query returns every employee, even if they are not assigned to a department.</p> </li> <li><u>RIGHT JOIN</u> (or RIGHT OUTER JOIN) returns all rows from the right table and the matching rows from the left table. If there is no match, the columns from the left table contain NULL <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>SELECT column, another_table_column, …
FROM mytable
RIGHT JOIN another_table
    ON mytable.id = another_table.id
WHERE condition(s)
ORDER BY column ASC/DESC
LIMIT num_limit OFFSET num_offset;
</code></pre></div> </div> <p>Example:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>SELECT employees.name, departments.department_name
FROM employees
RIGHT JOIN departments
    ON employees.department_id = departments.id;
</code></pre></div> </div> <p>This query returns every department, even if no employees belong to it. You can usually achieve the same result by swapping the table order and using a LEFT JOIN</p> </li> <li><u>FULL JOIN</u> (or FULL OUTER JOIN) returns all rows from both tables. Matching rows are combined, while non-matching rows from either table are included with NULL values for the missing side <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>SELECT column, another_table_column, …
FROM mytable
FULL JOIN another_table
    ON mytable.id = another_table.id
WHERE condition(s)
ORDER BY column ASC/DESC
LIMIT num_limit OFFSET num_offset;
</code></pre></div> </div> <p>Example:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>SELECT employees.name, departments.department_name
FROM employees
FULL JOIN departments
    ON employees.department_id = departments.id;
</code></pre></div> </div> <p>This query returns:</p> <ul> <li>employees with matching departments,</li> <li>employees without a department,</li> <li>departments without any employees. Note: MySQL does not support FULL JOIN directly. It can be simulated using a LEFT JOIN, a RIGHT JOIN, and UNION.</li> </ul> </li> </ul> <h3 id="expressions-and-data-transformations">Expressions and Data Transformations</h3> <ul> <li> <p>An <u>expression</u> is a calculation or transformation performed on one or more columns. Expressions are evaluated when the query runs and can be used to clean, format, or calculate new values without modifying the original data</p> </li> <li> <p>Expressions can be used in the <code class="language-plaintext highlighter-rouge">SELECT</code>, <code class="language-plaintext highlighter-rouge">WHERE</code>, <code class="language-plaintext highlighter-rouge">ORDER BY</code>, and other clauses</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>SELECT expression
FROM mytable;
</code></pre></div> </div> <p>Example:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>SELECT particle_speed / 2.0 AS half_particle_speed
FROM physics_data
WHERE ABS(particle_position) * 10.0 &gt; 500;
</code></pre></div> </div> </li> </ul> <h4 id="aliases-as">Aliases (<code class="language-plaintext highlighter-rouge">AS</code>)</h4> <ul> <li><u>AS</u> is used to assign a temporary name (alias) to a column, expression, or table</li> <li>Aliases make query results easier to read and simplify references in complex queries Column alias: <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>SELECT first_name AS employee_name
FROM employees;
</code></pre></div> </div> <p>Expression alias:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>SELECT salary * 1.10 AS increased_salary
FROM employees;
</code></pre></div> </div> <p>Table alias:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>SELECT e.name, d.department_name
FROM employees AS e
INNER JOIN departments AS d
    ON e.department_id = d.id;
</code></pre></div> </div> </li> </ul> <h4 id="common-expressions-for-data-cleaning">Common Expressions for Data Cleaning</h4> <p><u>Math Operations</u></p> <ul> <li>Performs calculations on numeric data</li> </ul> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>SELECT price * quantity AS total_cost
FROM orders;
</code></pre></div></div> <p>Common operators:</p> <ul> <li><code class="language-plaintext highlighter-rouge">+</code> Addition</li> <li><code class="language-plaintext highlighter-rouge">-</code> Subtraction</li> <li><code class="language-plaintext highlighter-rouge">*</code> Multiplication</li> <li><code class="language-plaintext highlighter-rouge">/</code> Division</li> <li><code class="language-plaintext highlighter-rouge">%</code> Modulus (remainder)</li> </ul> <p><u>Mathematical Functions</u></p> <table> <thead> <tr> <th>Function</th> <th>Description</th> <th>Example</th> </tr> </thead> <tbody> <tr> <td><code class="language-plaintext highlighter-rouge">ABS(x)</code></td> <td>Absolute value</td> <td><code class="language-plaintext highlighter-rouge">ABS(-5)</code> → <code class="language-plaintext highlighter-rouge">5</code></td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">ROUND(x, n)</code></td> <td>Round to <code class="language-plaintext highlighter-rouge">n</code> decimal places</td> <td><code class="language-plaintext highlighter-rouge">ROUND(price, 2)</code></td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">CEIL(x)</code></td> <td>Round up</td> <td><code class="language-plaintext highlighter-rouge">CEIL(3.2)</code> → <code class="language-plaintext highlighter-rouge">4</code></td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">FLOOR(x)</code></td> <td>Round down</td> <td><code class="language-plaintext highlighter-rouge">FLOOR(3.9)</code> → <code class="language-plaintext highlighter-rouge">3</code></td> </tr> </tbody> </table> <p>Example:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>SELECT ROUND(price, 2) AS rounded_price
FROM products;
</code></pre></div></div> <p><u>String Functions</u></p> <p>Useful for cleaning inconsistent text data</p> <table> <thead> <tr> <th>Function</th> <th>Description</th> </tr> </thead> <tbody> <tr> <td><code class="language-plaintext highlighter-rouge">UPPER()</code></td> <td>Converts text to uppercase</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">LOWER()</code></td> <td>Converts text to lowercase</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">TRIM()</code></td> <td>Removes leading and trailing spaces</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">LENGTH()</code></td> <td>Returns the length of a string</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">CONCAT()</code></td> <td>Combines multiple strings</td> </tr> </tbody> </table> <p>Examples:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>SELECT UPPER(first_name) AS first_name
FROM employees;
</code></pre></div></div> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>SELECT TRIM(email)
FROM customers;
</code></pre></div></div> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>SELECT CONCAT(first_name, ' ', last_name) AS full_name
FROM employees;
</code></pre></div></div> <h4 id="why-use-expressions">Why use expressions?</h4> <p>Expressions allow you to:</p> <ul> <li>Clean messy data (remove spaces, standardize capitalization)</li> <li>Perform calculations without changing the original data</li> <li>Create more readable output</li> <li>Avoid extra processing in another programming language (Python, Java, etc.)</li> </ul> <p>For example:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>SELECT
    TRIM(UPPER(first_name)) AS cleaned_name,
    ROUND(price * quantity, 2) AS total_cost
FROM orders;
</code></pre></div></div> <p>This query:</p> <ul> <li>Removes extra whitespace</li> <li>Converts names to uppercase</li> <li>Calculates the total order cost</li> <li>Gives each calculated value a descriptive alias</li> </ul> <h3 id="case-statements">Case Statements</h3> <ul> <li><u>CASE</u> is SQL’s version of an <code class="language-plaintext highlighter-rouge">if-else</code> statement. It evaluates conditions in order and returns a value for the first condition that is true</li> <li> <p>Commonly used to categorize data, create labels, or clean inconsistent values</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>SELECT
    column,
    CASE
        WHEN condition1 THEN result1
        WHEN condition2 THEN result2
        ELSE default_result
    END AS alias_name
FROM mytable;
</code></pre></div> </div> <p>Example:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>SELECT
    name,
    salary,
    CASE
        WHEN salary &gt;= 100000 THEN 'High'
        WHEN salary &gt;= 50000 THEN 'Medium'
        ELSE 'Low'
    END AS salary_category
FROM employees;
</code></pre></div> </div> <p>This query categorizes each employee’s salary as <strong>High</strong>, <strong>Medium</strong>, or <strong>Low</strong>.</p> <p><u>Common uses: </u></p> <ul> <li>Categorizing values into groups</li> <li>Replacing or standardizing values</li> <li>Handling missing or special cases</li> <li>Creating more readable output</li> </ul> </li> </ul> <h3 id="null-values">NULL Values</h3> <ul> <li><u>NULL</u> represents a <strong>missing or unknown value</strong></li> <li>It is <strong>not</strong> the same as <code class="language-plaintext highlighter-rouge">0</code>, an empty string (<code class="language-plaintext highlighter-rouge">''</code>), or <code class="language-plaintext highlighter-rouge">FALSE</code></li> <li>Because <code class="language-plaintext highlighter-rouge">NULL</code> means “unknown,” it cannot be compared using <code class="language-plaintext highlighter-rouge">=</code> or <code class="language-plaintext highlighter-rouge">!=</code></li> </ul> <p>Check for <code class="language-plaintext highlighter-rouge">NULL</code> values:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>SELECT *
FROM employees
WHERE manager_id IS NULL;
</code></pre></div></div> <p>Check for <code class="language-plaintext highlighter-rouge">non-NULL</code> values:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>SELECT *
FROM employees
WHERE manager_id IS NOT NULL;
</code></pre></div></div> <h4 id="working-with-null-values">Working with NULL Values</h4> <ul> <li>Use <u>COALESCE()</u> to replace <code class="language-plaintext highlighter-rouge">NULL</code> with a default value</li> <li><code class="language-plaintext highlighter-rouge">COALESCE()</code> returns the first non-<code class="language-plaintext highlighter-rouge">NULL</code> value from a list of expressions Example: <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>SELECT
    name,
    COALESCE(phone_number, 'No phone number') AS phone
FROM customers;
</code></pre></div> </div> <p>If <code class="language-plaintext highlighter-rouge">phone_number</code> is <code class="language-plaintext highlighter-rouge">NULL</code>, the query returns <code class="language-plaintext highlighter-rouge">"No phone number"</code> instead</p> </li> </ul> <h4 id="null-in-aggregate-functions">NULL in Aggregate Functions</h4> <p>Most aggregate functions ignore <code class="language-plaintext highlighter-rouge">NULL</code> values.</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>SELECT
    COUNT(*) AS total_rows,
    COUNT(phone_number) AS phone_numbers
FROM customers;
</code></pre></div></div> <ul> <li><code class="language-plaintext highlighter-rouge">COUNT(*)</code> counts <strong>all</strong> rows</li> <li><code class="language-plaintext highlighter-rouge">COUNT(phone_number)</code> counts only rows where <code class="language-plaintext highlighter-rouge">phone_number</code> is <strong>not</strong> <code class="language-plaintext highlighter-rouge">NULL</code></li> </ul> <p><u>Common Uses:</u></p> <ul> <li>Finding missing data (<code class="language-plaintext highlighter-rouge">IS NULL</code>)</li> <li>Filtering out missing values (<code class="language-plaintext highlighter-rouge">IS NOT NULL</code>)</li> <li>Replacing missing values (<code class="language-plaintext highlighter-rouge">COALESCE()</code>)</li> <li>Understanding how aggregate functions handle missing data</li> </ul> <h3 id="aggregations">Aggregations</h3> <ul> <li><u>Aggregate functions</u> summarize data from multiple rows into a single value</li> <li>They are commonly used to calculate totals, averages, minimums, maximums, and counts</li> <li> <p>Aggregate functions ignore <code class="language-plaintext highlighter-rouge">NULL</code> values (except <code class="language-plaintext highlighter-rouge">COUNT(*)</code>, which counts every row)</p> <p>Basic syntax:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>SELECT AGG_FUNC(column) AS alias
FROM mytable
WHERE condition;
</code></pre></div> </div> <p>Example:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>SELECT COUNT(*) AS total_employees
FROM employees;
</code></pre></div> </div> </li> </ul> <h4 id="common-aggregate-functions">Common Aggregate Functions</h4> <table> <thead> <tr> <th>Function</th> <th>Description</th> <th>Example</th> </tr> </thead> <tbody> <tr> <td><code class="language-plaintext highlighter-rouge">COUNT(*)</code></td> <td>Counts all rows</td> <td><code class="language-plaintext highlighter-rouge">COUNT(*)</code></td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">COUNT(column)</code></td> <td>Counts non-<code class="language-plaintext highlighter-rouge">NULL</code> values in a column</td> <td><code class="language-plaintext highlighter-rouge">COUNT(email)</code></td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">MIN(column)</code></td> <td>Returns the smallest value</td> <td><code class="language-plaintext highlighter-rouge">MIN(salary)</code></td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">MAX(column)</code></td> <td>Returns the largest value</td> <td><code class="language-plaintext highlighter-rouge">MAX(salary)</code></td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">AVG(column)</code></td> <td>Returns the average value</td> <td><code class="language-plaintext highlighter-rouge">AVG(salary)</code></td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">SUM(column)</code></td> <td>Returns the total of all values</td> <td><code class="language-plaintext highlighter-rouge">SUM(sales)</code></td> </tr> </tbody> </table> <p>Example:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>  SELECT
      COUNT(*) AS employees,
      AVG(salary) AS average_salary,
      MAX(salary) AS highest_salary
  FROM employees;
</code></pre></div></div> <h2 id="python">Python</h2> <p>Python is a multipurpose programming language that is used for analyzing data, automating, machine learning, and in many more cases.</p> <ul> <li>How does Python work? <ul> <li>Python acts as the orchestration layer in the ELT (Extract, Load, Transform) process. It executes SQL queries in the required order, while the database performs the data extraction, loading, and transformation. Python is responsible for coordinating the workflow, managing execution, and capturing the output and logs.</li> </ul> </li> <li>Where does Python run? <ul> <li>Python can run locally on your computer or a remote computer/server for production.</li> </ul> </li> </ul> <h3 id="apache-spark">Apache Spark</h3> <p><strong>What is Apache Spark?</strong></p> <ul> <li>Apache Spark is a distributed data processing framework used to scale Python, SQL, Java, and Scala data transformations across multiple machines</li> <li>It enables processing of very large datasets that cannot be handled efficiently on a single computer.</li> <li>Splits work across a cluster of machine to process, ingest, and transform data</li> </ul> <p><strong>When to use Apache Spark:</strong></p> <ul> <li>Processing large datasets (GBs to TBs or more)</li> <li>Scaling Python transformations across a cluster</li> <li>Distributed ETL pipelines</li> <li>Batch processing and streaming data</li> <li>Machine learning on large datasets using Spark MLlib</li> </ul> <p><strong>When Spark may not be necessary:</strong></p> <ul> <li>For small datasets that fit comfortably in memory, standard Python (e.g., pandas) is often simpler and faster.</li> <li>Spark introduces overhead, so it is generally most beneficial when data size or computation complexity justifies distributed processing.</li> </ul> ]]></content><author><name></name></author><category term="notes"/><summary type="html"><![CDATA[My learning progress + notes for data engineering Last Edited: August 3, 2026]]></summary></entry><entry><title type="html">Hello World</title><link href="https://yamajiang.github.io/blog/2026/welcome/" rel="alternate" type="text/html" title="Hello World"/><published>2026-07-10T15:47:00+00:00</published><updated>2026-07-10T15:47:00+00:00</updated><id>https://yamajiang.github.io/blog/2026/welcome</id><content type="html" xml:base="https://yamajiang.github.io/blog/2026/welcome/"><![CDATA[<p>Welcome to my portfolio website and blog!</p> <p>This space is used to help me document what I am learning, what I am building, and what I am thinking about over time.</p> <h3 id="what-this-blog-is-for">What This Blog Is For</h3> <p>I will use this blog for long-form updates and deeper write-ups, including, but not limited to :</p> <ul> <li>longer news updates</li> <li>project write-ups and build logs</li> <li>paper summaries and takeaways</li> <li>coursework explanations</li> <li>personal learning notes</li> </ul> <h3 id="what-i-am-learning-right-now">What I Am Learning Right Now</h3> <p>Right now, my goal is to self teach myself data engineering.</p> <p>I plan to use future posts to track my progress, share notes on tools and concepts, and reflect on what works (and what does not) as I learn.</p> <p>Thanks for reading !</p>]]></content><author><name></name></author><summary type="html"><![CDATA[Welcome !]]></summary></entry></feed>